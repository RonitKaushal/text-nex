# Lead Gen Desktop – copy this file to .env and edit as needed

# Local server port (default 39678)
PORT=39678

# Default backend URL (optional – user can also set after login via key)
# DEFAULT_BACKEND_URL=http://localhost:5000
